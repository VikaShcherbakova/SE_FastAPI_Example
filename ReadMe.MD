ReadMe
Релиз 2
🛠 Что нового в этом релизе:
Архитектурный рефакторинг:
Логика предсказаний вынесена в отдельный сервис app/model_service.py.
main.py теперь отвечает только за маршрутизацию API, используя импортированную функцию predict_sentiment.

Интерфейс пользователя (UI):
Добавлен файл streamlit.py — полноценное веб-приложение.
Реализовано кэширование модели (@st.cache_resource) для быстрой работы интерфейса.

Улучшение API:
Исправлен формат ответа на корневом эндпоинте / (теперь возвращается валидный JSON {"message": "..."}).
Все эндпоинты (GET и POST) переведены на использование единого сервиса обработки.

Конфигурация:
Обновлен .gitignore: добавлены исключения для виртуальных окружений (.venv, venv, env) и кэша Python.
